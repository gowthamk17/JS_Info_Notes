// to enforce private var use underscore for property and create getter and setter for them
// can't enforce them on language level 

// as for readOnly type property we only create getter setter will be ignored

// to use privete fields using # is still on proposal